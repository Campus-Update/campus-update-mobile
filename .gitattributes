# Files we do not hand-edit, exempted from git's whitespace check.
#
# The CI quality gate runs `git show --check`, which fails on trailing
# whitespace. Stripping it from these would either be undone the next time
# they are regenerated, or would alter a document that should stay verbatim.

# Generated by flutter_native_splash; regenerate with
#   dart run flutter_native_splash:create
android/app/src/main/res/values-v31/styles.xml       -whitespace
android/app/src/main/res/values-night-v31/styles.xml -whitespace

# The SIL Open Font License, reproduced as published.
assets/fonts/OFL.txt -whitespace
